spring:
  application:
    name: greenharvest
  datasource:
    url: jdbc:postgresql://localhost:5432/GreenHarvest
    username: postgres
    password: '24865'
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# JWT settings, injected via @Value in JwtService
app:
  jwt:

    secret: ${JWT_SECRET:NDQ4ZTFhMTgyOTE0NmU4NmYyN2M1ZDNkYzY0MGY4ZTFmZWQ0YTdhZjJhYTNjNzc4ZjM0MzcwNzhhZjMzZTk1Yg==}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}   # 15 minutes changed to 24 hours for testing purpose